name: A/B Visual Regression Tests

on:
  workflow_dispatch:
    inputs:
      tenant:
        description: "Governance tenant to test"
        required: true
        default: "optimism"
        type: choice
        options:
          - optimism
          - uniswap
          - ens
          - scroll
          - cyber
          - linea
          - b3
          - xai
          - derive
          - etherfi
          - syndicate
          - protocol-guild
          - towns
          - boost
      url_a:
        description: "URL A — Production baseline"
        required: true
        default: "https://vote.optimism.io"
        type: string
      url_b:
        description: "URL B — Preview or staging to compare"
        required: true
        type: string

      # ── Route Scopes (check the pages you want to scan) ──
      scan_index:
        description: "🏠 Index / Home page"
        required: false
        type: boolean
        default: true
      scan_proposals:
        description: "📋 Proposals listing"
        required: false
        type: boolean
        default: true
      scan_delegates:
        description: "👥 Delegates listing"
        required: false
        type: boolean
        default: true

      # ── Optional Filters ──
      target_proposals:
        description: "Specific proposal IDs (e.g. 116,adv-55). Leave empty for listing page."
        required: false
        type: string

jobs:
  visual-regression:
    name: "Visual A/B: ${{ inputs.tenant }}"
    runs-on: ubuntu-latest
    permissions:
      contents: "read"
      id-token: "write"

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      - name: Install Dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium

      - name: Build Route Scopes
        id: scopes
        run: |
          SCOPES=""
          if [ "${{ inputs.scan_index }}" = "true" ]; then SCOPES="${SCOPES:+$SCOPES,}index"; fi
          if [ "${{ inputs.scan_proposals }}" = "true" ]; then SCOPES="${SCOPES:+$SCOPES,}proposals"; fi
          if [ "${{ inputs.scan_delegates }}" = "true" ]; then SCOPES="${SCOPES:+$SCOPES,}delegates"; fi
          echo "target_types=$SCOPES" >> $GITHUB_OUTPUT
          echo "📍 Route scopes: $SCOPES"

      - name: Run A/B Regression Engine
        continue-on-error: true
        env:
          URL_A: ${{ inputs.url_a || 'https://vote.optimism.io' }}
          URL_B: ${{ inputs.url_b }}
          TARGET_PROPOSALS: ${{ inputs.target_proposals }}
          TARGET_TYPES: ${{ steps.scopes.outputs.target_types }}
          TENANT_CONTEXT: ${{ inputs.tenant }}
        run: npm run test:ab

      - name: Google Auth
        if: always()
        id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}

      - name: Set up Cloud SDK
        if: always()
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: "silent-turbine-390703"

      - name: Upload Artifacts to GCP Bucket
        if: always()
        env:
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REF_NAME: ${{ github.ref_name }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          chmod +x scripts/upload-ab-artifacts.sh
          ./scripts/upload-ab-artifacts.sh
