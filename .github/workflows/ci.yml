name: CI
run-name: ${{ github.actor }} - ${{ github.event_name }} - ${{ github.repository }}
on:
  pull_request:
    branches:
      - 'main'
  push:
    branches:
      - 'main'

jobs:
  swagger_lint:
    runs-on: ubuntu-latest
    name: Swagger Editor Validator Remote

    steps:
      - uses: actions/checkout@v2

      - name: Install Swagger dependencies
        run: |
          yarn install
          yarn add @swagger-api/apidom-parser-adapter-api-design-systems-json

      - name: Debug node_modules
    run: |
      ls -la node_modules/@swagger-api/apidom-parser-adapter-api-design-systems-json
      if [ -f node_modules/@swagger-api/apidom-parser-adapter-api-design-systems-json/package.json ]; then
        cat node_modules/@swagger-api/apidom-parser-adapter-api-design-systems-json/package.json
      else
        echo "package.json not found in module directory"
      fi
      if [ -f node_modules/@swagger-api/apidom-parser-adapter-api-design-systems-json/src/adapter.cjs ]; then
        echo "adapter.cjs found"
      else
        echo "adapter.cjs not found in module directory"
      fi

      - name: Validate OpenAPI definition
        uses: char0n/apidom-validate@v1
        with:
          fails-on: 2
          definition-file: spec/oas_v1.yaml

  lint-prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: yarn install
      - name: Lint with Prettier
        run: yarn check-prettier
