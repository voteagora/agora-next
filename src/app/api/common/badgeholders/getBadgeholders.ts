import { cache } from "react";

export async function isBadgeholder(address: string) {
  // TODO: implement query for checking if a given address is a badgeholder
  // for viewing and voting on ballots
  return true;
}
