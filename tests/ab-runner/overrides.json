{
  "general": {
    "ignoreSelectors": [],
    "expectDiff": false
  },
  "routes": {
    "/delegates": {
      "ignoreSelectors": ["[data-testid='delegate-card'] .font-mono", "img"],
      "expectDiff": false,
      "reason": "We expect delegates view to be structurally identical between A and B, but images and dynamic scores might randomly fetch."
    },
    "/proposals": {
      "ignoreSelectors": [".dynamic-time", "[data-testid='proposal-status']"],
      "expectDiff": false
    }
  }
}
